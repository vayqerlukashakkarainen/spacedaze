{
	"name": "spacedaze",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\""
	},
	"dependencies": {
		"kaplay": "4000.0.0-alpha.18"
	},
	"devDependencies": {
		"@types/node": "^22.15.19",
		"vite": "^6.3.5"
	}
}
